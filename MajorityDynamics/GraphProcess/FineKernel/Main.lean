import MajorityDynamics.GraphProcess.FineKernel.Markov

/-! Section 2 Proposition 2.3: the universal fine kernel and the actual state chain.
`K` is constructed from the literal independent uniform component law.
`present_transition` and `past_transition` are the actual finite event identities.
-/
