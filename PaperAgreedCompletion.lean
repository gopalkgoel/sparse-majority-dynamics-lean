import MajorityDynamics.Paper.Final.Checks

/-! Completion under the agreed policy: exact original final theorem types,
with only standard foundations and the explicitly accepted cited inputs.
The imported Checks module is also checked by the default PaperAxiomAudit target.
`PaperCompletion.lean` separately retains the stronger foundations-only goal. -/
